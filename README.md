# fractal-tasks-template

Template for a Fractal-compatible Python package of tasks

## Instructions

git clone ...

replace all what is needed

python -m venv venv
source venv/bin/activate
pip install -e .[dev]

pytest -v


python -m build

